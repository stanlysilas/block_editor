name: block_editor_example
description: Example application for the block_editor package.
publish_to: none
resolution: workspace
version: 0.0.4-dev.1

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  block_editor:
    path: ../packages/block_editor
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true